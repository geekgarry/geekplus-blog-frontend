# 生产环境配置
ENV=production

# 管理系统/生产环境
VUE_APP_BASE_API=/pro-api

BASE_URL=/

# 注意：不要设置 VUE_CLI_BABEL_TRANSPILE_MODULES
# 该变量只要存在（即使是 false 字符串）也会在 JS 里被当成 true，
# 从而启用 dynamic-import-node，毁掉生产环境 import() 分包。
